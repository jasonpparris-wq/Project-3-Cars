# Azure ML workspace configuration
# Referenced by the get-config job in deploy-model-training-pipeline-classical.yml
# Update these values to match your Azure subscription before running the workflow.

resource_group: your-resource-group-name
aml_workspace: your-aml-workspace-name
